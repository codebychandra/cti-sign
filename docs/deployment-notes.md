# Deployment Notes

This project is deployed from the `main` branch. If the live app shows an older layout after a code change, retry the latest Cloudflare deployment or create a fresh commit on `main` to trigger a rebuild.

The project workspace uses top tabs on the project detail page:

- Template
- Form
- Completed
- Setting
